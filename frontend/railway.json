{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "npm ci && npm run build:railway"
    },
    "deploy": {
      "startCommand": "npx serve -s build -l ${PORT:-3000}",
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 10
    }
  }